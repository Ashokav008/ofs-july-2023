let username = 'Alex';
let gender = 'Male';
//Using back tick for string and expressions
console.log(`Name = ${username}, Gender = ${gender}`);
console.log('Name = '+username+', Gender = '+gender);
console.log('Name = ${username}, Gender = ${gender}');

// create an array of javascript objects having properties like
// username, gender and address, then address must be a nested object with properties like
// state, city and pin, using for loop and template strings print each objects 
// present in the array
// create atleast 3 objects in the array